const slides=[...document.querySelectorAll('.slide')];let i=0;const bar=document.querySelector('#bar'),num=document.querySelector('#num'),hint=document.querySelector('#hint');function show(n){i=Math.max(0,Math.min(slides.length-1,n));slides.forEach((s,k)=>{s.classList.toggle('on',k===i);const f=s.querySelector('.foot span:last-child');if(f)f.textContent=String(k+1).padStart(2,'0')+' / '+slides.length});bar.style.width=((i+1)/slides.length*100)+'%';num.textContent=(i+1)+' / '+slides.length;if(i)hint.style.opacity='.0'}addEventListener('keydown',e=>{if(['ArrowRight','PageDown',' '].includes(e.key))show(i+1);else if(['ArrowLeft','PageUp'].includes(e.key))show(i-1);else if(e.key==='Home')show(0);else if(e.key==='End')show(slides.length-1)});addEventListener('click',e=>{if(!e.target.closest('a,button'))show(i+1)});show(0);
